export type ProjectProps = {
    id: string,
    title: string,
    description: string,
    repoLink: string,
};

export type ExperienceProps = {
    id: string,
    description : string,
};

export type PersonalInfo = {
    name: string,
    contactEmail: string,
    points: string,
    degree: string,
    personalImageLink: string,
}