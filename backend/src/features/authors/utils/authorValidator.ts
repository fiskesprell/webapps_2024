import { Author } from "../types/author";

export const canEdit = (project: Author, userId: string): boolean =>
    // Ikke implementert
    true

export const isValidAuthor = (project: Author): boolean =>
    // Ikke implementert
    true