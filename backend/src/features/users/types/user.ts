export type User = {
    id: string;
    email: string;
    name: string;
    role: string | null;
};

export type DbUser = {
    id: string;
    email: string;
    name: string;
    role: string | null;
};