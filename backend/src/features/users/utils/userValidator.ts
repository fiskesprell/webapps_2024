import { User } from "../types/user"

export const canEdit = (user: User, userId: string): boolean =>
    // Ikke implementert 
    true
  
export const isValidUser = (user: User ): boolean =>
    // Ikke implementert
    true